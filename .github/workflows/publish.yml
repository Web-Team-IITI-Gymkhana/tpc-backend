name: Publish Image

on:
  release:
    types: 
      - published
  workflow_dispatch:

env:
  DOCKER_REPO: lofowebteam/tpc_portal

jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4
      
      - name: Log in to Docker Hub
        uses: docker/login-action@v3.0.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v5.0.0
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ env.DOCKER_REPO }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ env.DOCKER_REPO }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_REPO }}:buildcache,mode=max


### To-do bump sha into the another repo

  # bump_tag:
  #   name: Bump tag into docker-compose.yml
  #   needs: push_to_registry
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Check out the repo
  #       uses: actions/checkout@v4
  #       with:
  #         repository: tpc-pipeline-testing
  #         ref: 'master'
  #         token:  ${{ secrets.ACTIONS_GITHUB_TOKEN }}
  # 
  #     - name: setup git config
  #       run: |
  #         git config user.name "GitHub Actions Bot"
  #         git config user.email "<>"
  # 
  #     - name: change tag
  #       run:|
  #         # sed command
  #     - run: git push origin master
